import { Typography, Box } from "@mui/material";

const Home = () => {
  return (
    <Box component="section" sx={{ p: 2, border: "1px dashed grey" }}>
      <Typography>This Box renders as an HTML section element. Home</Typography>
    </Box>
  );
};

export default Home;
